{
  "name": "semantic-notes",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "pnpm --dir app dev",
    "build": "pnpm --dir app build",
    "test": "ts-node tests/rewriteNamespace.test.ts && pnpm --dir app test"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  }
}
